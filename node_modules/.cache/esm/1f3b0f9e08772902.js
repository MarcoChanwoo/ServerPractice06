let mongoose,mongo;_087‍.x([["default",()=>_087‍.o]]);_087‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}],["mongo",["mongo"],function(v){mongo=v}]]);

const { Schema } = mongoose;
const PostSchema = new Schema({
    title: String,
    body: String,
    tags: [String],
    publishedDate: {
        type: Date,
        default: Date.now,
    },
});

const Post = mongoose.model('Post', PostSchema);
_087‍.d(Post);
